<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <!-- Res Config Settings: Universal Fuel Price Configuration -->
    <record id="res_config_settings_view_form_farm_inherit" model="ir.ui.view">
        <field name="name">res.config.settings.view.form.farm.inherit</field>
        <field name="model">res.config.settings</field>
        <field name="priority">50</field>
        <field name="inherit_id" ref="hr_payroll.res_config_settings_view_form"/>
        <field name="arch" type="xml">
            <xpath expr="//block[@id='hr_payroll_settings']" position="inside">
                <setting id="fuel_price_setting" string="Universal Fuel Price / Liter" help="Universal daily market fuel price in Birr per liter used to calculate Grade 18 (50L) and Grade 19+ (60L) transport allowances across all contracts.">
                    <field name="fuel_price_per_liter" class="w-25"/><span>Birr / Liter</span>
                </setting>
            </xpath>
        </field>
    </record>
</odoo>
